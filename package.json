{
  "name": "sample_client",
  "version": "0.0.1+b",
  "description": "Barebone Electron client that loads a URL from config.json",
  "main": "main.js",
  "author": {
    "name": "Francesco Galasso",
    "email": "info@alfadispenser.com"
  },
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "electron .",
    "dist": "electron-builder --publish=never",
    "dist:linux:arm64": "electron-builder --linux --arm64 --publish=never",
    "dist:win": "electron-builder --win --publish=never"
  },
  "dependencies": {},
  "devDependencies": {
    "electron": "^39.0.0",
    "electron-builder": "^26.0.0"
  },
  "build": {
    "appId": "barebone-electron-client",
    "productName": "MyElectronClient",
    "files": [
      "main.js",
      "config.json",
      "renderer/**/*"
    ],
    "directories": {
      "output": "dist"
    },
    "linux": {
      "target": [
        "deb"
      ],
      "category": "Utility"
    },
    "deb": {
      "depends": [
        "zlib1g"
      ]
    },
    "win": {
      "target": [
        "nsis"
      ]
    }
  }
}

